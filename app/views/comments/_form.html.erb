<%= form_for ([@post, @post.comments.build]) do |f|%>
  <p>
    <%= f.label :name, "Nombre"%> <br>
    <%= f.text_field :name%>
  </p>
  <p>
    <%= f.label :body, "Comentario"%> <br>
    <%= f.text_area :body%> <br>
  </p>
  <br>
  <%= f.submit %>
<% end%>